# 项目协作流程

本项目采用“方案评审 -> 当前 agent 执行 -> 结果检查 -> 下一步推进”的协作方式。所有在本项目中工作的 agent 都必须遵守以下边界。

## 角色分工

- 当前方案层 agent：负责理解需求、分析现状、拆分任务、设计实现方案、识别风险和制定验证标准。
- 用户：负责评审方案并明确批准、修改或暂停。未经用户明确批准，不得进入执行阶段。
- 当前 agent：仅在用户批准后负责按已批准方案修改代码、运行验证并返回结果。
- 执行完成后：检查变更是否符合方案，核对关键验证结果，指出问题或残留风险；不得无依据地宣称执行成功。

## 强制流程

1. 先读取相关代码、配置、测试和项目状态，形成简洁、可评审的方案。
2. 方案必须说明目标、涉及文件或模块、实现步骤、验证方式、风险和待用户确认事项。
3. 在用户明确批准前，方案层 agent 不得修改项目代码或运行会改变项目状态的执行步骤。
4. 用户批准后，当前 agent 按完整方案直接实现。实现必须包含明确范围、输入、预期结果、验证要求，以及不得修改无关文件的约束。
5. 实现完成后，当前 agent 做轻量但有证据的检查：查看 diff、检查相关文件、核对测试或构建输出，必要时补充只读诊断。
6. 检查完成后，向用户汇报当前完成内容、检查结论、未解决问题或风险，并给出下一步流程。若发现偏差，先说明偏差并重新提出处理方案，不得自行扩大范围。

## 每轮沟通格式

方案阶段应包含：

- 当前理解
- 拟解决的问题
- 实现方案
- 影响范围与风险
- 验证方式
- 请用户评审的事项

执行结果阶段应包含：

- 当前已完成内容
- 实际执行结果
- 已检查的证据
- 问题、风险或验证缺口
- 下一步建议

## 约束

- 方案层和执行层职责分离；不要把“提出方案”描述成“已经实现”。
- 只围绕用户批准的范围推进，保留用户已有改动，不擅自回滚或覆盖无关内容。
- 任务必须边界清晰、可验证、可回收；共享状态和最终整合由当前 agent 负责把关。
- 不在 prompt、日志、文件或提交中暴露密钥、令牌及其他敏感凭据。
- 所有结论以实际代码、diff、测试和构建结果为依据；无法验证的内容要明确标注。
- 每次执行结果汇报后都必须说明下一步，不让流程停留在“已返回结果”。
